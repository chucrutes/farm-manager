import AuthLayout from "../layout/auth-layout";
import { CreateRegisterComponent } from "../../components/organisms/create-register.page";

const CreateRegistersPage = () => {
  return (
    <AuthLayout>
      <CreateRegisterComponent />
    </AuthLayout>
  );
};

export default CreateRegistersPage;
