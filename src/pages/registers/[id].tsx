import { RegisterDetailComponent } from "../../components/organisms/register-detail.page";
import AuthLayout from "../layout/auth-layout";

const RegisterDetailPage = () => {
  return (
    <AuthLayout>
      <RegisterDetailComponent />
    </AuthLayout>
  );
};

export default RegisterDetailPage;
