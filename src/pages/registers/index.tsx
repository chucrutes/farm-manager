import AuthLayout from "../layout/auth-layout";
import { RegistersComponent } from "../../components/organisms/registers.page";

const RegistersPage = () => {
  return (
    <AuthLayout>
      <RegistersComponent />
    </AuthLayout>
  );
};

export default RegistersPage;
